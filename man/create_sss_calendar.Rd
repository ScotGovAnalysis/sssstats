% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_sss_calendar.R
\name{create_sss_calendar}
\alias{create_sss_calendar}
\title{create_sss_calendar Creates the Social Security Scotland calendar}
\usage{
create_sss_calendar(date_from = "2018-01-01", date_to = "2070-01-01")
}
\arguments{
\item{date_from}{A character string or \code{Date} representing the start date
for the calendar. Defaults to \code{"2018-01-01"}.}

\item{date_to}{A character string or \code{Date} representing the end date
for the calendar. Defaults to \code{"2070-01-01"}.}
}
\value{
A \code{Calendar} object (S3 class) from the \strong{bizdays} package, containing the
specified holidays, weekend structure, and date bounds.
}
\description{
create_sss_calendar Creates the Social Security Scotland calendar
}
\details{
Constructs a \code{bizdays} calendar object used for Social Security Scotland
processing time calculations. The calendar defines business days as all days
except Glasgow and Dundee joint bank holidays (hard‑coded within the function)
and weekends (Saturday and Sunday).
The calendar can be created for any date range, and is typically
used when calculating processing times.
The function uses a hard‑coded list of Scottish bank holidays for the years
2018–2027. If \code{date_to} is after the last available holiday in the
list, the function emits a warning to prompt the user to update the holiday vector.

This function wraps \code{bizdays::create.calendar()} and returns the created
calendar object.
}
\examples{
\dontrun{
# Create calendar for default range
calendar <- create_sss_calendar("2018-01-01", "2026-12-31")
}

}
